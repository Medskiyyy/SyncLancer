/**
 * Global Vitest setup file.
 *
 * Each test file declares its own vi.mock() calls at the top level for precise control.
 * This file is intentionally minimal — it only applies shared configuration that must
 * run before every test suite.
 */

// Extend expect matchers here if needed, e.g.:
// import '@testing-library/jest-dom';
